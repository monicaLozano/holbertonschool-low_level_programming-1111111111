#include "holberton.h"

/**
 * swap_int - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
*a = 98;
*b = 42
if (a != b)
{
*a += *b;
*b = *a - *b;
*a -= *b;
}
}
