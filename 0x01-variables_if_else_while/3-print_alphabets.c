#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <ctype.h>
/**
* main - positive negative zero
*
* Return: always zero
*/
int main(void)
{
char i;
char j;
for (i = 'a' ; i <= 'z' ; i++)
for (i = 'A' ; i <= 'Z' ; i++)
{
putchar(i);
putchar(J);
}
putchar('\n');
return (0);
}
