#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - positive negative zero
*
* Return: always zero
*/
int main(void)
{
char i;
for (i = 'a' ; i <= 'z' ; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
